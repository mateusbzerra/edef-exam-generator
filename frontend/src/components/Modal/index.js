import React from 'react';
import { Container } from 'react-bootstrap';

function ModalComponent({ children, className, data, visible, handleClose }) {
  return (
    <div>
      <Container></Container>
    </div>
  );
}

export default ModalComponent;
